apiVersion: v1
kind: {{ .Values.mediawiki.volume.pvc.kind }}
metadata:
  name: {{ .Values.mediawiki.volume.pvc.name }}
spec:
  accessModes:
    - {{ .Values.mediawiki.volume.pvc.accessMode }}
  resources:
    requests:
      storage: {{ .Values.mediawiki.volume.pvc.storage }}
